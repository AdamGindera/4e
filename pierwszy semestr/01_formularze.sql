
-- 2. Dodaj sekcję:

 

-- A. nagłówek Aktualizacja

-- B. Formularz z obsługiwany przez plik aktualizacja.php z polem id ucznia i przyciskiem Wyślij
 

-- 3.  Dodaj sekcję:

 

-- A. nagłówek Usuwanie
-- B. Formularz z obsługiwany przez plik usuwanie.php z polem id ucznia i przyciskiem Wyślij
-- 4. Przygotuj zapytania, wykonaj i zapisz w pliku kwerendy.txt:

 

-- A. Zmieniające miasto urodzenia ucznia o id 10 na Bydgoszcz
-- B.  Usuwające ucznia o id 26
-- 5. plik aktualizacja.php

 

-- A. pobiera dane z pierwszego formularza (id ucznia)
-- B. wykonuje zmodyfikowane zapytanie 4A dla pobranego id ucznia
-- C. Wyświetla informację o prawidłowym wykonaniu zapytania
-- D. wyświetla link powracający do głównej strony
 

-- 6. plik usuwanie.php

 

-- A. pobiera dane z pierwszego formularza (id ucznia)
-- B. wykonuje zmodyfikowane zapytanie 4B dla pobranego id ucznia
-- C. Wyświetla informację o prawidłowym wykonaniu zapytania
-- D. wyświetla link powracający do głównej strony
 

-- 7*. Dodaj walidację:

--  A. przed wykonaniem zapytań powinno być sprawdzane, czy istnieje w bazie uczeń o tym id. Jeśli nie - powinien być wyświetlony odpowiedni komunikat

-- 8* Zamiast pola do wpisania id powinna być wyświetlona lista rozwijana, na której będzie wyświetlone imię, nazwisko oraz klasa ucznia. Wartością wybranej opcji powinno być id ucznia